# GENERATED FILE — do not edit by hand. Source of truth is pyproject.toml
# ([project] deps + [tool.pixi]). Regenerate with:
#   pixi workspace export conda-environment -e dev > environment.yml
# (then restore this header + the `name: cstar-forge-env` dev-setup.sh expects).
#
# Note on the `pip:` section: it lists every pypi-dependencies entry from
# pyproject.toml's [project] deps/extras, including several (roms-tools, dask,
# distributed, numpy, xarray, pandas, netcdf4, pyyaml) that are ALSO installed
# from conda-forge above. This is expected and harmless: `pip install` only
# acts when a requirement isn't already satisfied, so for a fresh
# `conda env create -f environment.yml` those conda-installed versions already
# satisfy the pip specifiers and pip leaves them alone. The pip section is what
# actually pulls in packages with no conda-forge presence in this manifest yet
# (cstar-ocean, copernicusmarine, gdown, f90nml, jupyterlab/voila/papermill/
# ipywidgets/matplotlib, pytest*/ruff/pre-commit) plus `-e .` (cstar-forge
# itself, editable).
name: cstar-forge-env
channels:
- conda-forge
- nodefaults
dependencies:
- python >=3.12,<3.14
- roms-tools >=4.0.0,<5
- dask *
- distributed *
- numpy *
- xarray *
- pandas *
- netcdf4 *
- kerchunk *
- fastparquet *
- nest-asyncio *
- ujson *
- pip
- pip:
  - pytest
  - pytest-asyncio
  - pytest-cov
  - pytest-xdist
  - ruff>=0.12.2
  - pre-commit>=3.8
  - netcdf4
  - f90nml
  - jupyterlab
  - voila
  - papermill
  - ipywidgets
  - matplotlib
  - -e .
  - jinja2
  - pandas
  - pyyaml>=6.0
  - copernicusmarine
  - gdown
  - dask
  - distributed
  - threadpoolctl
  - fsspec
  - pydantic
  - numpy
  - xarray
  - nbformat
  - psutil
  - cstar-ocean>=0.9
  - roms-tools>=4.0.0, <5
